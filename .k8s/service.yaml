apiVersion: v1
kind: Service
metadata:
  name: nyu-project
spec:
  type: ClusterIP
  selector:
    app: nyu-project
  internalTrafficPolicy: Local
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080